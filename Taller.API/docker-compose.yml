version: '3.4'

services:

  sqldata:
    image: mcr.microsoft.com/mssql/server:latest 
    ports:
    - "1433:1433"
    restart: always
    environment:
        ACCEPT_EULA: "Y"
        SA_PASSWORD: "Strong(!)Password"
        MSSQL_PID: "Developer"